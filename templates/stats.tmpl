{{ template "base" . }}

{{ define "stats" }}

<div id="stats-modal" class="hidden fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm flex items-center justify-center z-50">
  <div id="stats-modal-content" class="p-6 rounded-3xl shadow-xl w-full max-w-md glowing-effect show transition-all duration-300 relative text-base"
       style="background-color: #1C3D63; color: white;">
       
    <button onclick="closeModal()" class="absolute top-3 right-4 text-4xl text-gray-300 hover:text-white" title="Close">
      &times;
    </button>

    <h2 id="modal-title" class="text-3xl font-bold mb-5 text-center text-white">📊 Platform Stats</h2>

    <div id="stats-content" class="grid grid-cols-2 gap-4 text-xl">
      <div id="stats-loader" class="hidden col-span-2 flex justify-center my-4">
        <div class="animate-spin rounded-full h-10 w-10 border-4 border-purple-500 border-t-transparent"></div>
      </div>
    </div>
  </div>
</div>
<script src="/static/components/stats.js"></script>
{{ end }}
