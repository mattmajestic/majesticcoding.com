{{ template "base" . }}

<!DOCTYPE html>
<html lang="en">
<body>
    <div class="container mx-auto p-4">
    <h1 class="text-4xl font-bold mb-16">📊 Dashboard</h1>
  
    <div class="flex flex-row items-start gap-8 mt-4">
      {{ template "dash-side" . }}

      <!-- Metrics & Platform Metrics (60%) -->
      {{ template "metrics" . }}
    </div>
  </div>
  {{template "footer_home" . }}
  
  <script src="/static/components/dashboard.js"></script>
  <script src="/static/components/theme.js"></script>
</body>
</html>
