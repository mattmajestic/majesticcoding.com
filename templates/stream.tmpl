{{ template "base" . }}

{{ define "stream" }}
<div class="px-4 py-4 w-full">
  <div class="w-full max-w-[90%] mx-auto items-center">
    <div class="flex justify-center items-center p-4">
      <video
        id="video"
        controls
        autoplay
        playsinline
        class="rounded-xl shadow-xl"
        width="1280"
        height="720"
        style="max-width: 100%; height: auto;"
      ></video>
    </div>
    <div id="offline-message" class="hidden flex flex-col items-center justify-center text-red-500 text-lg text-center">
      <h1 class="mb-8 font-bold text-3xl">🚫 Stream Offline</h1>
      <p class="text-md">Feel free to check out the socials or docs</p>
      <p class="text-md">in the meantime</p>
      <p class="mb-8 text-xl">👇 </p>
      {{ template "socials" . }}
      <p class="mb-8"></p>
      {{ template "docs-btn" . }}
      {{ template "dash-btn" . }}
    </div>
    <h1 class="mt-10 flex text-xs font-italics items-center justify-center gap-4">
      <span class="text-lg font-bold">Powered by</span>
        <img src="https://upload.wikimedia.org/wikipedia/commons/9/93/Amazon_Web_Services_Logo.svg"
             alt="AWS Logo"
             class="h-8 w-auto inline-block"
             style="background: transparent;" />
        <span class="text-lg font-bold">IVS</span>
      </h1>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
  <script src="/static/components/stream.js"></script>

</div>
{{ end }}
