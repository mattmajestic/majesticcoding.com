{{ define "rtmp" }}
<div class="p-6 border border-gray-700 rounded-lg bg-gray-800 text-gray-300">
  <h2 class="text-2xl font-bold text-white mb-2">📡 RTMP Streaming with NGINX & Docker</h2>
  <h2 class="text-xl font-bold mb-2 text-blue-300">
    Stream live content from OBS to your site using an RTMP server powered by NGINX inside Docker.
  </h2>
  <ul class="list-disc list-inside text-md">
    <li>RTMP ingestion on port <code>1935</code> from OBS</li>
    <li>HLS (HTTP Live Streaming) output served over <code>8081</code></li>
    <li>Volume shared between RTMP container and Go app</li>
    <li>Docker Compose for simple orchestration</li>
  </ul>
  <div class="mt-4 bg-gray-900 p-3 rounded">
    <pre class="text-green-400 text-sm"><code>services:
  rtmp-server:
    image: alfg/nginx-rtmp
    ports:
      - "1935:1935"
      - "8081:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - stream_data:/tmp/hls

  majesticcoding:
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - stream_data:/static/stream</code></pre>
  </div>
</div>
{{ end }}
