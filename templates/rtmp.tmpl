{{ define "rtmp" }}
<div class="p-6 border border-gray-700 rounded-lg bg-gray-800 text-gray-300">
  <h2 class="text-2xl font-bold text-white mb-2">🎥 Streaming from OBS to AWS IVS</h2>
  <h2 class="text-xl font-bold mb-2 text-blue-300">
    Stream live content from OBS directly to AWS IVS, just like you would to Twitch, for reliable, scalable, and low-latency video delivery.
  </h2>
  <ul class="list-disc list-inside text-md mb-4">
    <li>Use OBS to broadcast to your AWS IVS RTMP endpoint—no extra server setup required.</li>
    <li>Enjoy the same backend reliability and performance as Twitch, but with your own branding and controls.</li>
    <li>Automatic HLS output and playback for web and mobile viewers.</li>
    <li>Easy integration with your Go app and multi-cloud infrastructure.</li>
  </ul>
  <div class="mt-4 bg-gray-900 p-3 rounded">
    <h3 class="font-bold text-blue-300 mb-2">Terraform: Provision AWS IVS Channel</h3>
    <pre class="text-green-400 text-sm"><code># Terraform: AWS IVS Channel for Live Streaming
resource "aws_ivs_channel" "live_stream" {
  name        = "majestic-stream"
  type        = "STANDARD"
  latency_mode = "LOW"
  recording_configuration_arn = aws_ivs_recording_configuration.recording.arn
}

resource "aws_ivs_recording_configuration" "recording" {
  name = "majestic-recording"
  destination_configuration {
    s3 {
      bucket_name = "my-ivs-recordings"
    }
  }
}
</code></pre>
  </div>
</div>

{{ end }}